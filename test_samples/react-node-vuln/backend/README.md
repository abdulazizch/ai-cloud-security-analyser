# Vulnerable Express Server (local testing only)
- Open CORS, reflected XSS, command injection, hardcoded secrets, insecure "SQL" string building.
- Start: `npm install` then `npm start` (runs on :4000).
